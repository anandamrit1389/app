export enum PresentationAccess {
  PUBLIC = 'Public',
  PRIVATE = 'Private',
}

export enum SlideTransition {
  NONE = 'none',
  SLIDE_BELOW = 'slide',
  SLIDE_RIGHT = 'slide-right',
  FADE_IN = 'fade',
}

export enum ImagePurpose {
  PRESENTATION_IMAGE = 'presentation_image',
  CUSTOM_LOGO = 'custom_logo',
  CONVERTED_SLIDE = 'converted_slide',
}
